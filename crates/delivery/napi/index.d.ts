/* auto-generated by NAPI-RS */
/* eslint-disable */
export declare class BundleStore {
  constructor(kind: string, platform: string, storageDir: string);
  ensure(): Promise<BundleStatus>;
  status(): BundleStatus;
}

export declare class DeliveryClient {
  static connect(url: string, options?: ConnectOptions | undefined | null): Promise<DeliveryClient>;
  deviceId(): string;
  meta(): Promise<any | null>;
  discoverManifest(rootUrl: string): Promise<any>;
  applyVersion(channel: string, version: string, rootUrl: string): Promise<void>;
  push(kind: string, artifact: string | Buffer, options?: PushOptions | undefined | null): Promise<Phase>;
  installWebapp(bundle: string | Buffer, provenance?: string | undefined | null): Promise<InstalledWebapp>;
  switchWebapp(id: string): Promise<string>;
  nextEvent(): Promise<UpdateEvent>;
  closed(): Promise<void>;
}

export declare class Discovery {
  constructor();
  endpoints(): Array<Endpoint>;
  nextChange(): Promise<EndpointChange>;
}

export interface BundleStatus {
  state: string;
  version?: string;
  received?: number;
  total?: number;
  reason?: string;
  path?: string;
}

export interface CompositeVersion {
  daemon: string;
  image: string;
}

export interface ConnectOptions {
  deviceId?: string;
  cacheDir?: string;
  dataDir?: string;
  appName?: string;
  appVersion?: string;
}

export interface Endpoint {
  id: string;
  url: string;
  host: string;
  nickname?: string;
}

export interface EndpointChange {
  kind: string;
  endpoint: Endpoint;
}

export declare function initLogging(directive?: string | undefined | null): void;

export interface InstalledWebapp {
  id: string;
  name: string;
  version: string;
  provenance?: string;
}

export declare function parseCompositeVersion(raw: string): CompositeVersion | null;

export interface Phase {
  kind: string;
  asset?: string;
  received?: number;
  sent?: number;
  total?: number;
  ratePerSec?: number;
  etaSeconds?: number;
  writePercent?: number;
  reason?: string;
}

export interface PlanStep {
  id: number;
  kind: string;
  label: string;
  bytes: number;
}

export interface PushOptions {
  updateUrlBase?: string;
  zcks?: Record<string, string>;
  label?: string;
  version?: string;
}

export interface UpdateEvent {
  kind: string;
  deviceId?: string;
  updateKind?: string;
  release?: string;
  daemonVersion?: string;
  imageVersion?: string;
  version?: string;
  updatedAt?: string;
  reason?: string;
  stepId?: number;
  steps?: Array<PlanStep>;
  phase?: Phase;
}
