// THIS FILE IS GENERATED BY tools/codegen. Do not edit by hand.
// Re-generate with `just codegen` (or `just rust`).

use bridgething_sdk_runtime::{RequestFailure, SdkError};
use futures::{Stream, StreamExt, future::ready};
use libbridgething::{gateway::*, *};
use tokio_stream::wrappers::BroadcastStream;

use crate::Gateway;

impl Gateway {
  /// The `Audio` surface.
  pub fn audio(&self) -> AudioSurface<'_> {
    AudioSurface(self)
  }
  /// The `Geo` surface.
  pub fn geo(&self) -> GeoSurface<'_> {
    GeoSurface(self)
  }
  /// The `Library` surface.
  pub fn library(&self) -> LibrarySurface<'_> {
    LibrarySurface(self)
  }
  /// The `Net` surface.
  pub fn net(&self) -> NetSurface<'_> {
    NetSurface(self)
  }
  /// The `Notifications` surface.
  pub fn notifications(&self) -> NotificationsSurface<'_> {
    NotificationsSurface(self)
  }
  /// The `Phone` surface.
  pub fn phone(&self) -> PhoneSurface<'_> {
    PhoneSurface(self)
  }
  /// The `Player` surface.
  pub fn player(&self) -> PlayerSurface<'_> {
    PlayerSurface(self)
  }
  /// The `System` surface.
  pub fn system(&self) -> SystemSurface<'_> {
    SystemSurface(self)
  }
  /// The `Tunnel` surface.
  pub fn tunnel(&self) -> TunnelSurface<'_> {
    TunnelSurface(self)
  }
  /// The `Voice` surface.
  pub fn voice(&self) -> VoiceSurface<'_> {
    VoiceSurface(self)
  }
  /// The `Webapp` surface.
  pub fn webapp(&self) -> WebappSurface<'_> {
    WebappSurface(self)
  }
  /// The `Asset` surface.
  pub fn asset(&self) -> AssetSurface<'_> {
    AssetSurface(self)
  }
  /// The `Authority` surface.
  pub fn authority(&self) -> AuthoritySurface<'_> {
    AuthoritySurface(self)
  }
  /// The `Capabilities` surface.
  pub fn capabilities(&self) -> CapabilitiesSurface<'_> {
    CapabilitiesSurface(self)
  }
  /// The `Chrome` surface.
  pub fn chrome(&self) -> ChromeSurface<'_> {
    ChromeSurface(self)
  }
  /// The `Time` surface.
  pub fn time(&self) -> TimeSurface<'_> {
    TimeSurface(self)
  }
}

/// Methods scoped to the `Audio` wire surface.
pub struct AudioSurface<'a>(&'a Gateway);

impl<'a> AudioSurface<'a> {
  pub async fn tts_started(&self, payload: TtsStarted) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeAudioMsgEvent::TtsStarted(payload)).await
  }
  pub async fn tts_ended(&self, payload: TtsEnded) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeAudioMsgEvent::TtsEnded(payload)).await
  }
  pub async fn volume_changed(&self, payload: VolumeChanged) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeAudioMsgEvent::VolumeChanged(payload)).await
  }
}

/// Methods scoped to the `Geo` wire surface.
pub struct GeoSurface<'a>(&'a Gateway);

impl<'a> GeoSurface<'a> {
  pub async fn position(&self, payload: Position) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeGeoMsgEvent::Position(payload)).await
  }
}

/// Methods scoped to the `Library` wire surface.
pub struct LibrarySurface<'a>(&'a Gateway);

impl<'a> LibrarySurface<'a> {
  pub async fn favorite_changed(&self, payload: FavoriteChanged) -> Result<(), SdkError> {
    self
      .0
      .event(GatewayToBridgeLibraryMsgEvent::FavoriteChanged(payload))
      .await
  }
}

/// Methods scoped to the `Net` wire surface.
pub struct NetSurface<'a>(&'a Gateway);

impl<'a> NetSurface<'a> {
  pub async fn ws_message(&self, payload: NetWsMessage) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeNetMsgEvent::WsMessage(payload)).await
  }
  pub async fn ws_closed(&self, payload: NetWsClosed) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeNetMsgEvent::WsClosed(payload)).await
  }
  pub async fn ws_error_event(&self, payload: NetWsErrorEvent) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeNetMsgEvent::WsErrorEvent(payload)).await
  }
  pub async fn stream_begin(&self, payload: StreamBegin) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeNetMsgEvent::StreamBegin(payload)).await
  }
  pub async fn stream_chunk(&self, payload: StreamChunk) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeNetMsgEvent::StreamChunk(payload)).await
  }
  pub async fn stream_end(&self, payload: StreamEnd) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeNetMsgEvent::StreamEnd(payload)).await
  }
  pub async fn stream_error(&self, payload: StreamError) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeNetMsgEvent::StreamError(payload)).await
  }
}

/// Methods scoped to the `Notifications` wire surface.
pub struct NotificationsSurface<'a>(&'a Gateway);

impl<'a> NotificationsSurface<'a> {
  pub async fn posted(&self, payload: Notification) -> Result<(), SdkError> {
    self
      .0
      .event(GatewayToBridgeNotificationsMsgEvent::Posted(payload))
      .await
  }
  pub async fn updated(&self, payload: Notification) -> Result<(), SdkError> {
    self
      .0
      .event(GatewayToBridgeNotificationsMsgEvent::Updated(payload))
      .await
  }
  pub async fn removed(&self, payload: NotificationRemoved) -> Result<(), SdkError> {
    self
      .0
      .event(GatewayToBridgeNotificationsMsgEvent::Removed(payload))
      .await
  }
  /// Stream of `Notifications` events. Broadcast lag is dropped silently. The
  /// stream is `Unpin`, so callers can `.next().await` without pinning.
  pub fn events(&self) -> impl Stream<Item = BridgeToGatewayNotificationsMsgEvent> + 'static {
    BroadcastStream::new(self.0.events()).filter_map(|msg| {
      ready(match msg {
        Ok(msg) => match msg.data {
          BridgeToGatewayMsgData::Notifications(inner) => inner.into_event(),
          _ => None,
        },
        Err(_) => None,
      })
    })
  }
}

/// Methods scoped to the `Phone` wire surface.
pub struct PhoneSurface<'a>(&'a Gateway);

impl<'a> PhoneSurface<'a> {
  pub async fn snapshot(&self, payload: PhoneStateReply) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgePhoneMsgEvent::Snapshot(payload)).await
  }
  pub async fn communications_snapshot(&self, payload: CommunicationsSnapshot) -> Result<(), SdkError> {
    self
      .0
      .event(GatewayToBridgePhoneMsgEvent::CommunicationsSnapshot(payload))
      .await
  }
  pub async fn call_started(&self, payload: PhoneCall) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgePhoneMsgEvent::CallStarted(payload)).await
  }
  pub async fn call_updated(&self, payload: PhoneCall) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgePhoneMsgEvent::CallUpdated(payload)).await
  }
  pub async fn call_ended(&self, payload: PhoneCallEnded) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgePhoneMsgEvent::CallEnded(payload)).await
  }
}

/// Methods scoped to the `Player` wire surface.
pub struct PlayerSurface<'a>(&'a Gateway);

impl<'a> PlayerSurface<'a> {
  pub async fn snapshot(&self, payload: PlayerState) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgePlayerMsgEvent::Snapshot(payload)).await
  }
  pub async fn delta(&self, payload: NowPlayingUpdate) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgePlayerMsgEvent::Delta(payload)).await
  }
  pub async fn queue_changed(&self, payload: QueueSnapshot) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgePlayerMsgEvent::QueueChanged(payload)).await
  }
  /// Stream of `Player` events. Broadcast lag is dropped silently. The
  /// stream is `Unpin`, so callers can `.next().await` without pinning.
  pub fn events(&self) -> impl Stream<Item = BridgeToGatewayPlayerMsgEvent> + 'static {
    BroadcastStream::new(self.0.events()).filter_map(|msg| {
      ready(match msg {
        Ok(msg) => match msg.data {
          BridgeToGatewayMsgData::Player(inner) => inner.into_event(),
          _ => None,
        },
        Err(_) => None,
      })
    })
  }
}

/// Methods scoped to the `System` wire surface.
pub struct SystemSurface<'a>(&'a Gateway);

impl<'a> SystemSurface<'a> {
  pub async fn ota_chunk(&self, payload: OtaChunk) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeSystemMsgEvent::OtaChunk(payload)).await
  }
  pub async fn ota_abandon(&self, payload: OtaAbandon) -> Result<(), SdkError> {
    self
      .0
      .command(GatewayToBridgeSystemMsgCommand::OtaAbandon(payload))
      .await
  }
  pub async fn cancel_update(&self) -> Result<(), SdkError> {
    self.0.command(GatewayToBridgeSystemMsgCommand::CancelUpdate).await
  }
  pub async fn ota_asset_range_chunk(&self, payload: OtaAssetRangeChunk) -> Result<(), SdkError> {
    self
      .0
      .event(GatewayToBridgeSystemMsgEvent::OtaAssetRangeChunk(payload))
      .await
  }
  pub async fn ota_begin(&self, request: OtaBegin) -> Result<OtaBeginAck, RequestFailure<OtaBeginRejected>> {
    self.0.request(request).await
  }
  pub async fn device_get_nickname(&self) -> Result<DeviceNicknameReply, RequestFailure<::core::convert::Infallible>> {
    self.0.request(DeviceGetNickname).await
  }
  pub async fn device_set_nickname(
    &self,
    request: DeviceSetNickname,
  ) -> Result<DeviceNicknameReply, RequestFailure<DeviceNicknameRejected>> {
    self.0.request(request).await
  }
  /// Stream of `System` events. Broadcast lag is dropped silently. The
  /// stream is `Unpin`, so callers can `.next().await` without pinning.
  pub fn events(&self) -> impl Stream<Item = BridgeToGatewaySystemMsgEvent> + 'static {
    BroadcastStream::new(self.0.events()).filter_map(|msg| {
      ready(match msg {
        Ok(msg) => match msg.data {
          BridgeToGatewayMsgData::System(inner) => inner.into_event(),
          _ => None,
        },
        Err(_) => None,
      })
    })
  }
}

/// Methods scoped to the `Tunnel` wire surface.
pub struct TunnelSurface<'a>(&'a Gateway);

impl<'a> TunnelSurface<'a> {
  pub async fn data(&self, payload: TunnelData) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeTunnelMsgEvent::Data(payload)).await
  }
  pub async fn closed(&self, payload: TunnelClosed) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeTunnelMsgEvent::Closed(payload)).await
  }
}

/// Methods scoped to the `Voice` wire surface.
pub struct VoiceSurface<'a>(&'a Gateway);

impl<'a> VoiceSurface<'a> {
  pub async fn mic_open(&self, payload: VoiceMicOpen) -> Result<(), SdkError> {
    self.0.command(GatewayToBridgeVoiceMsgCommand::MicOpen(payload)).await
  }
  pub async fn mic_close(&self) -> Result<(), SdkError> {
    self.0.command(GatewayToBridgeVoiceMsgCommand::MicClose).await
  }
  pub async fn dispatch(&self, payload: VoiceDispatch) -> Result<(), SdkError> {
    self.0.command(GatewayToBridgeVoiceMsgCommand::Dispatch(payload)).await
  }
  /// Stream of `Voice` events. Broadcast lag is dropped silently. The
  /// stream is `Unpin`, so callers can `.next().await` without pinning.
  pub fn events(&self) -> impl Stream<Item = BridgeToGatewayVoiceMsgEvent> + 'static {
    BroadcastStream::new(self.0.events()).filter_map(|msg| {
      ready(match msg {
        Ok(msg) => match msg.data {
          BridgeToGatewayMsgData::Voice(inner) => inner.into_event(),
          _ => None,
        },
        Err(_) => None,
      })
    })
  }
}

/// Methods scoped to the `Webapp` wire surface.
pub struct WebappSurface<'a>(&'a Gateway);

impl<'a> WebappSurface<'a> {
  pub async fn install_chunk(&self, payload: WebappInstallChunk) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeWebappMsgEvent::InstallChunk(payload)).await
  }
  pub async fn install_abandon(&self, payload: WebappInstallAbandon) -> Result<(), SdkError> {
    self
      .0
      .command(GatewayToBridgeWebappMsgCommand::InstallAbandon(payload))
      .await
  }
  pub async fn list(&self) -> Result<WebappList, RequestFailure<::core::convert::Infallible>> {
    self.0.request(ListWebapps).await
  }
  pub async fn get_active(&self) -> Result<WebappActive, RequestFailure<::core::convert::Infallible>> {
    self.0.request(GetActiveWebapp).await
  }
  pub async fn switch_to(&self, request: WebappSwitchTo) -> Result<WebappActive, RequestFailure<WebappError>> {
    self.0.request(request).await
  }
  pub async fn install_begin(
    &self,
    request: WebappInstallBegin,
  ) -> Result<WebappInstallBeginAck, RequestFailure<WebappError>> {
    self.0.request(request).await
  }
  pub async fn uninstall(&self, request: WebappUninstall) -> Result<WebappActive, RequestFailure<WebappError>> {
    self.0.request(request).await
  }
  pub async fn icon(&self, request: WebappIcon) -> Result<WebappIconReply, RequestFailure<WebappError>> {
    self.0.request(request).await
  }
  pub async fn config_get(
    &self,
    request: WebappConfigGet,
  ) -> Result<WebappConfigGetReply, RequestFailure<WebappError>> {
    self.0.request(request).await
  }
  pub async fn config_list(
    &self,
    request: WebappConfigList,
  ) -> Result<WebappConfigListReply, RequestFailure<WebappError>> {
    self.0.request(request).await
  }
  pub async fn config_set(&self, request: WebappConfigSet) -> Result<WebappConfigAck, RequestFailure<WebappError>> {
    self.0.request(request).await
  }
  pub async fn config_delete(
    &self,
    request: WebappConfigDelete,
  ) -> Result<WebappConfigAck, RequestFailure<WebappError>> {
    self.0.request(request).await
  }
  /// Stream of `Webapp` events. Broadcast lag is dropped silently. The
  /// stream is `Unpin`, so callers can `.next().await` without pinning.
  pub fn events(&self) -> impl Stream<Item = BridgeToGatewayWebappMsgEvent> + 'static {
    BroadcastStream::new(self.0.events()).filter_map(|msg| {
      ready(match msg {
        Ok(msg) => match msg.data {
          BridgeToGatewayMsgData::Webapp(inner) => inner.into_event(),
          _ => None,
        },
        Err(_) => None,
      })
    })
  }
}

/// Methods scoped to the `Asset` wire surface.
pub struct AssetSurface<'a>(&'a Gateway);

impl<'a> AssetSurface<'a> {
  pub async fn push(&self, payload: AssetPush) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeAssetMsgEvent::Push(payload)).await
  }
  pub async fn clear(&self, payload: AssetClear) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeAssetMsgEvent::Clear(payload)).await
  }
  pub async fn push_chunk(&self, payload: AssetPushChunk) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeAssetMsgEvent::PushChunk(payload)).await
  }
  pub async fn push_abandon(&self, payload: AssetPushAbandon) -> Result<(), SdkError> {
    self
      .0
      .command(GatewayToBridgeAssetMsgCommand::PushAbandon(payload))
      .await
  }
  pub async fn push_begin(
    &self,
    request: AssetPushBegin,
  ) -> Result<AssetPushBeginAck, RequestFailure<AssetPushBeginRejected>> {
    self.0.request(request).await
  }
}

/// Methods scoped to the `Authority` wire surface.
pub struct AuthoritySurface<'a>(&'a Gateway);

impl<'a> AuthoritySurface<'a> {
  pub async fn claim(&self, payload: AuthorityClaim) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeAuthorityMsgEvent::Claim(payload)).await
  }
  pub async fn release(&self, payload: AuthorityRelease) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeAuthorityMsgEvent::Release(payload)).await
  }
}

/// Methods scoped to the `Capabilities` wire surface.
pub struct CapabilitiesSurface<'a>(&'a Gateway);

impl<'a> CapabilitiesSurface<'a> {
  pub async fn announce(&self, payload: GatewayCapabilities) -> Result<(), SdkError> {
    self
      .0
      .event(GatewayToBridgeCapabilitiesMsgEvent::Announce(payload))
      .await
  }
}

/// Methods scoped to the `Chrome` wire surface.
pub struct ChromeSurface<'a>(&'a Gateway);

impl<'a> ChromeSurface<'a> {
  pub async fn navigate(&self, payload: ChromeNavigate) -> Result<(), SdkError> {
    self.0.command(GatewayToBridgeChromeMsgCommand::Navigate(payload)).await
  }
}

/// Methods scoped to the `Time` wire surface.
pub struct TimeSurface<'a>(&'a Gateway);

impl<'a> TimeSurface<'a> {
  pub async fn snapshot(&self, payload: TimeInfo) -> Result<(), SdkError> {
    self.0.event(GatewayToBridgeTimeMsgEvent::Snapshot(payload)).await
  }
}
