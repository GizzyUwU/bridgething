#!/usr/bin/env bash
set -euo pipefail

PORT="${1:-9222}"
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/device.sh"

exec ssh \
    -L "${PORT}:127.0.0.1:9222" \
    -N -T \
    -o ServerAliveInterval=10 \
    "${DEVICE_SSH_OPTS[@]}" \
    "root@${DEVICE_HOST}"
