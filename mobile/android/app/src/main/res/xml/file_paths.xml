<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
      Only the generated log bundles are exposed. Exports are written under
      cacheDir/exports so a stale bundle the user never shared is reclaimable
      by the platform under storage pressure.
    -->
    <cache-path name="log-exports" path="exports/" />
</paths>
