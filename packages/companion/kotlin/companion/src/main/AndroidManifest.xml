<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!--
      Permissions the companion library needs to do its job. The host app
      still has to declare these in its own manifest (gradle merges them
      in) and request the runtime ones (location) before the geo
      dispatcher can return real fixes.
    -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
  <!--
      Telephony surface. The host app must request the default-dialer role
      (RoleManager.ROLE_DIALER) before the OS binds the InCallService; the
      runtime perms (CALL_PHONE, ANSWER_PHONE_CALLS, READ_CALL_LOG) gate
      placing / answering / redial respectively.
    -->
  <uses-permission android:name="android.permission.CALL_PHONE" />
  <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
  <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
  <uses-permission android:name="android.permission.READ_PHONE_STATE" />
  <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
  <uses-permission android:name="android.permission.READ_CALL_LOG" />
  <!--
      Companion-app telephony: with MANAGE_ONGOING_CALLS the OS binds our InCallService
      WITHOUT the default-dialer role, unlocking the full control surface (hold / swap /
      merge / mute / DTMF) for a CDM companion. It is signature|appop, so there is no
      runtime dialog: grant via `adb shell appops set <pkg> MANAGE_ONGOING_CALLS allow`
      (or a CDM device-profile association). When it (or the dialer role) is absent, the
      PhoneStateWatcher light path covers observe + answer/decline/end.
    -->
  <uses-permission android:name="android.permission.MANAGE_ONGOING_CALLS" />

  <application>
    <service
      android:name="com.bridgething.companion.BridgethingInCallService"
      android:permission="android.permission.BIND_INCALL_SERVICE"
      android:exported="true">
      <meta-data
        android:name="android.telecom.IN_CALL_SERVICE_UI"
        android:value="true" />
      <intent-filter>
        <action android:name="android.telecom.InCallService" />
      </intent-filter>
    </service>
  </application>
</manifest>