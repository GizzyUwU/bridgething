// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { StockSetPreset } from './stock';

export type ClientBluetoothCommand =
  | { action: 'list' }
  | { action: 'connect'; args: { mac: string } }
  | { action: 'scan' }
  | { action: 'enableDiscoverable' }
  | { action: 'disableDiscoverable' }
  | { action: 'pair'; args: { mac: string } }
  | { action: 'forget'; args: { mac: string } }
  | { action: 'enablePAN'; args: { mac: string } }
  | { action: 'disablePAN'; args: { mac: string } }
  | { action: 'setAlias'; args: { name: string } };

export type ClientCommand = { id: string } & (
  | ({ type: 'bluetooth' } & ClientBluetoothCommand)
  | ({ type: 'storage' } & ClientStorageCommand)
  | ({ type: 'system' } & ClientSystemCommand)
  | ({ type: 'voice' } & ClientVoiceCommand)
  | ({ type: 'interaction'; stock_msg_id: number | null } & (
      | { action: 'getImage'; args: { id: string } }
      | { action: 'getThumbnailImage'; args: { id: string } }
      | { action: 'getNextTracks' }
      | { action: 'phoneAnswer' }
      | { action: 'phoneDecline' }
      | { action: 'phoneCallImage'; args: { phone_number: string } }
      | { action: 'phoneCallMessage'; args: { phone_number: string; message: string } }
      | { action: 'increaseVolume' }
      | { action: 'decreaseVolume' }
      | { action: 'skipToIndex'; args: { index: number } }
      | { action: 'skipNext' }
      | { action: 'skipPrev'; args: { allow_seeking: boolean } }
      | { action: 'seekTo'; args: { position: number } }
      | { action: 'pause' }
      | { action: 'resume' }
      | { action: 'setShuffle'; args: { shuffle: boolean } }
      | { action: 'setRepeat'; args: { repeat_mode: boolean } }
      | { action: 'spotifyGetChildren'; args: { parent_id: string; limit: number; offset: number | null } }
      | { action: 'spotifyGetPodcast'; args: { uri: string; limit: number | null; offset: number | null } }
      | { action: 'spotifyGetSaved'; args: { id: string } }
      | { action: 'spotifyPlayPodcastTrailer'; args: { uri: string } }
      | { action: 'spotifyQueueUri'; args: { uri: string } }
      | { action: 'spotifySetPodcastPlaybackSpeed'; args: { playback_speed: number } }
      | { action: 'spotifySetSaved'; args: { id: string | null; uri: string | null; saved: boolean } }
      | {
          action: 'spotifyPlayUri';
          args: {
            uri: string;
            feature_identifier: string;
            interaction_id: string | null;
            skip_to_uri: string | null;
            skip_to_uid: string | null;
          };
        }
      | { action: 'spotifyGetPermissions' }
      | { action: 'spotifySummonDj' }
      | { action: 'spotifyGetHome'; args: { limit: number; limit_overrides: { [key in string]?: number } } }
      | { action: 'spotifyGetPresets' }
      | { action: 'spotifySetPreset'; args: { presets: Array<StockSetPreset> } }
      | { action: 'spotifyGetTips' }
      | { action: 'spotifyGetTts'; args: { file: string } }
    ))
);

export type ClientCommandType =
  | ({ type: 'bluetooth' } & ClientBluetoothCommand)
  | ({ type: 'storage' } & ClientStorageCommand)
  | ({ type: 'system' } & ClientSystemCommand)
  | ({ type: 'voice' } & ClientVoiceCommand)
  | ({ type: 'interaction'; stock_msg_id: number | null } & (
      | { action: 'getImage'; args: { id: string } }
      | { action: 'getThumbnailImage'; args: { id: string } }
      | { action: 'getNextTracks' }
      | { action: 'phoneAnswer' }
      | { action: 'phoneDecline' }
      | { action: 'phoneCallImage'; args: { phone_number: string } }
      | { action: 'phoneCallMessage'; args: { phone_number: string; message: string } }
      | { action: 'increaseVolume' }
      | { action: 'decreaseVolume' }
      | { action: 'skipToIndex'; args: { index: number } }
      | { action: 'skipNext' }
      | { action: 'skipPrev'; args: { allow_seeking: boolean } }
      | { action: 'seekTo'; args: { position: number } }
      | { action: 'pause' }
      | { action: 'resume' }
      | { action: 'setShuffle'; args: { shuffle: boolean } }
      | { action: 'setRepeat'; args: { repeat_mode: boolean } }
      | { action: 'spotifyGetChildren'; args: { parent_id: string; limit: number; offset: number | null } }
      | { action: 'spotifyGetPodcast'; args: { uri: string; limit: number | null; offset: number | null } }
      | { action: 'spotifyGetSaved'; args: { id: string } }
      | { action: 'spotifyPlayPodcastTrailer'; args: { uri: string } }
      | { action: 'spotifyQueueUri'; args: { uri: string } }
      | { action: 'spotifySetPodcastPlaybackSpeed'; args: { playback_speed: number } }
      | { action: 'spotifySetSaved'; args: { id: string | null; uri: string | null; saved: boolean } }
      | {
          action: 'spotifyPlayUri';
          args: {
            uri: string;
            feature_identifier: string;
            interaction_id: string | null;
            skip_to_uri: string | null;
            skip_to_uid: string | null;
          };
        }
      | { action: 'spotifyGetPermissions' }
      | { action: 'spotifySummonDj' }
      | { action: 'spotifyGetHome'; args: { limit: number; limit_overrides: { [key in string]?: number } } }
      | { action: 'spotifyGetPresets' }
      | { action: 'spotifySetPreset'; args: { presets: Array<StockSetPreset> } }
      | { action: 'spotifyGetTips' }
      | { action: 'spotifyGetTts'; args: { file: string } }
    ));

export type ClientInteractionCommand =
  | { action: 'getImage'; args: { id: string } }
  | { action: 'getThumbnailImage'; args: { id: string } }
  | { action: 'getNextTracks' }
  | { action: 'phoneAnswer' }
  | { action: 'phoneDecline' }
  | { action: 'phoneCallImage'; args: { phone_number: string } }
  | { action: 'phoneCallMessage'; args: { phone_number: string; message: string } }
  | { action: 'increaseVolume' }
  | { action: 'decreaseVolume' }
  | { action: 'skipToIndex'; args: { index: number } }
  | { action: 'skipNext' }
  | { action: 'skipPrev'; args: { allow_seeking: boolean } }
  | { action: 'seekTo'; args: { position: number } }
  | { action: 'pause' }
  | { action: 'resume' }
  | { action: 'setShuffle'; args: { shuffle: boolean } }
  | { action: 'setRepeat'; args: { repeat_mode: boolean } }
  | { action: 'spotifyGetChildren'; args: { parent_id: string; limit: number; offset: number | null } }
  | { action: 'spotifyGetPodcast'; args: { uri: string; limit: number | null; offset: number | null } }
  | { action: 'spotifyGetSaved'; args: { id: string } }
  | { action: 'spotifyPlayPodcastTrailer'; args: { uri: string } }
  | { action: 'spotifyQueueUri'; args: { uri: string } }
  | { action: 'spotifySetPodcastPlaybackSpeed'; args: { playback_speed: number } }
  | { action: 'spotifySetSaved'; args: { id: string | null; uri: string | null; saved: boolean } }
  | {
      action: 'spotifyPlayUri';
      args: {
        uri: string;
        feature_identifier: string;
        interaction_id: string | null;
        skip_to_uri: string | null;
        skip_to_uid: string | null;
      };
    }
  | { action: 'spotifyGetPermissions' }
  | { action: 'spotifySummonDj' }
  | { action: 'spotifyGetHome'; args: { limit: number; limit_overrides: { [key in string]?: number } } }
  | { action: 'spotifyGetPresets' }
  | { action: 'spotifySetPreset'; args: { presets: Array<StockSetPreset> } }
  | { action: 'spotifyGetTips' }
  | { action: 'spotifyGetTts'; args: { file: string } };

export type ClientStorageCommand =
  | { action: 'get'; args: { key: string } }
  | { action: 'put'; args: { key: string; value: string } }
  | { action: 'delete'; args: { key: string } };

export type ClientSystemCommand =
  | { action: 'versionRequest' }
  | { action: 'reboot' }
  | { action: 'powerOff' }
  | { action: 'factoryReset' }
  | { action: 'phoneCallAccept'; args: { call_id: string } }
  | { action: 'phoneCallEnd'; args: { call_id: string } }
  | { action: 'legacyStockReturnToSpotify' }
  | { action: 'legacyStockRemoteConfigurationRequest' };

export type ClientVoiceCommand =
  | { action: 'cancel' }
  | { action: 'pushToTalk' }
  | { action: 'muteMic'; args: { preserve: boolean } }
  | { action: 'unmuteMic'; args: { preserve: boolean } };
